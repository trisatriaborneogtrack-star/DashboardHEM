# ============================================================
# Contoh secrets. JANGAN commit file secrets.toml yang asli.
#
# Lokal : salin file ini jadi .streamlit/secrets.toml
# Cloud : tempel isinya di App settings -> Secrets (bukan di repo)
# ============================================================

# --- Sheet sudah di-share "Anyone with the link -> Viewer" ---
# Ini saja sudah cukup. Tab dicari berdasarkan NAMA, jadi gid tidak diperlukan.
# Nama tab harus sama persis dengan di Google Sheet (peka huruf besar/kecil).
[gsheet]
sheet_id = "1I30t7uLOzwBMVyq0k-Rfy1NTzGUFLbT9v37XeFjKbF0"
worksheet_responses = "Form Responses 1"
worksheet_roster = "Sheet1"


# ============================================================
# HANYA kalau sheet TIDAK boleh dibuka publik.
# Kalau sheet sudah publik, JANGAN isi blok di bawah — hapus saja.
# Kesalahan tersering: nilai contoh belum diganti, gejalanya
#   "Unable to load PEM file ... Invalid symbol 95"
#
# private_key: pakai tiga tanda kutip, tiap baris berdiri sendiri.
# Di file JSON kuncinya satu baris panjang dengan \n; ubah tiap \n
# itu jadi baris baru sungguhan.
# ============================================================
# [gcp_service_account]
# type = "service_account"
# project_id = "SALIN_DARI_JSON"
# private_key_id = "SALIN_DARI_JSON"
# private_key = """-----BEGIN PRIVATE KEY-----
# SALIN_SELURUH_ISI_KUNCI_DARI_JSON
# -----END PRIVATE KEY-----
# """
# client_email = "SALIN_DARI_JSON@project.iam.gserviceaccount.com"
# client_id = "SALIN_DARI_JSON"
# auth_uri = "https://accounts.google.com/o/oauth2/auth"
# token_uri = "https://oauth2.googleapis.com/token"
# auth_provider_x509_cert_url = "https://www.googleapis.com/oauth2/v1/certs"
# client_x509_cert_url = "SALIN_DARI_JSON"
# universe_domain = "googleapis.com"
